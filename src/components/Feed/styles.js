const styles = () => {
  return {
    loading: {
      height: '100vh',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    },
    feedContainer: {
      paddingTop: 40,
    },
    fetchingMore: {
      paddingTop: 40,
      display: 'flex',
      justifyContent: 'center',
    },
  }
}

export default styles

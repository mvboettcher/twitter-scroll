import { render } from '@testing-library/react'
import QuotedTweetCard from './index'

describe('QuotedTweetCard', () => {
  test('render QuotedTweetCard component', async () => {
    render(<QuotedTweetCard />)
  })
})
